name: language_helper
description: Multi-language app tool with an efficient generator and a custom GPT-4 translator for easy localization.
version: 0.10.0-rc
homepage: https://github.com/lamnhan066/language_helper

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  language_code: ^0.4.0
  language_helper_generator: ^0.4.2
  shared_preferences: ^2.2.2
  args: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

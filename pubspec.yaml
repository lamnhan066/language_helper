name: language_helper
description: Easy way to implement multiple languages (localizations) into your app with minimal effort.
version: 0.8.0
homepage: https://github.com/lamnhan066/language_helper

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  language_code: ^0.3.1
  language_helper_generator: ^0.3.0
  shared_preferences: ^2.2.2
  args: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
